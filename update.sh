# Pull latest release of monitor-cloud-quota tool
git fetch && git reset --hard origin/latest-release

# # Install any additional system requirements
# sudo apt-get update
# while read -r line; do sudo apt-get -y install "$line"; done < ubuntu-deps.txt
# 
# # Install any additional python requirements
# pip install -r python-deps.txt
